[project]
name = "django-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "automium-web>=0.1.1",
    "blog-app>=0.1.3",
    "django>=6.0",
    "django-jazzmin>=3.0.2",
    "djangorestframework>=3.16.1",
    "djangorestframework-stubs>=3.16.8",
    "include>=0.2.2",
    "pillow>=12.1.1",
    "pre-commit>=4.5.0",
]

[project.scripts]
django-project = "django_project:main"

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"
